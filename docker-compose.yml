version: '3.9'

services:
  redis:
    image: "redis:alpine"
    container_name: "redis"
    ports:
      - "6379:6379"
  bot:
    build: .
    container_name: "currency_bot"
    env_file:
      - .env
    environment:
      - INSIDE_DOCKER=true
    depends_on:
      - redis
